
export class Player {
    name: string;
    email: string;
    phoneNumber: number;
    role: string;
	// Game Registered and own ranking and opponent ranking (array of array)
    // gamesInfo: string[][];
    // 	days in week (array)
    // days: string[];
 }